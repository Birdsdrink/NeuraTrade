# [TEMPLATE]
# Local development: FastAPI backend running on this machine.
# Use the computer's LAN IP (not localhost) so a phone on the same network can reach it.
EXPO_PUBLIC_API_BASE_URL=http://YOUR_COMPUTER_LAN_IP:8000/api

# Deployed backend (only usable when this machine has working internet):
# EXPO_PUBLIC_API_BASE_URL=https://neuratrade-a9xvnbozg-datalinks-projects.vercel.app/api
