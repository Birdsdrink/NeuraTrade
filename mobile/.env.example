# Production: NeuraTrade backend deployed on Vercel (REST works; live WebSocket ticks are
# unavailable on serverless, so candles refresh via the built-in REST polling fallback).
EXPO_PUBLIC_API_BASE_URL=https://neuratrade-dhches75n-datalinks-projects.vercel.app/api

# For local development against the FastAPI backend on this machine, replace with:
# EXPO_PUBLIC_API_BASE_URL=http://YOUR_COMPUTER_LAN_IP:8000/api
